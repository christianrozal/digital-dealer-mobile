import { View, Text } from "react-native";
// Comment out problematic imports until we need them
// import { useSelector } from "react-redux";
// import { selectConsultant } from "@/store/consultantSlice";

const HomeScreen = () => {
  return (
    <View>
      <Text>Home Screen</Text>
    </View>
  );
};

export default HomeScreen;